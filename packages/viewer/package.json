{
  "private": true,
  "name": "viewer",
  "version": "0.0.0",
  "description": "",
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "scripts": {
    "dev": "npm run start",
    "start": "react-scripts start",
    "build": "rm -rf ../../build && cp -rf ./public ../../build && esbuild src/index.tsx --bundle --outfile=../../build/index.js --loader:.html=text --loader:.js=jsx --loader:.svg=dataurl",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^5.16.1",
    "@testing-library/react": "^12.1.2",
    "@testing-library/user-event": "^13.5.0",
    "@types/jest": "^27.4.0",
    "@types/react": "^17.0.38",
    "@types/react-dom": "^17.0.11",
    "@types/react-table": "^7.7.9",
    "react-scripts": "5.0.0",
    "web-vitals": "^2.1.2"
  }
}

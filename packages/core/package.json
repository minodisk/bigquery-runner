{
  "name": "core",
  "version": "1.0.0",
  "description": "",
  "license": "Apach-2",
  "main": "src/index.ts",
  "scripts": {
    "test": "jest --silent=false",
    "test:watch": "npm run test -- --watchAll"
  },
  "jest": {
    "roots": [
      "<rootDir>/src"
    ],
    "testMatch": [
      "**/?(*.)+(spec|test).+(ts|tsx|js)"
    ],
    "transform": {
      "^.+\\.(ts|tsx)$": "ts-jest"
    }
  }
}

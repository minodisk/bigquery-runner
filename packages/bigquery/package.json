{
  "name": "bigquery",
  "version": "1.0.0",
  "description": "",
  "license": "Apach-2",
  "main": "src/bigquery.ts",
  "scripts": {
    "test": "jest --silent=false",
    "test:watch": "npm run test -- --watch"
  },
  "jest": {
    "roots": [
      "<rootDir>/src"
    ],
    "testMatch": [
      "**/__tests__/**/*.+(ts|tsx|js)",
      "**/?(*.)+(spec|test).+(ts|tsx|js)"
    ],
    "transform": {
      "^.+\\.(ts|tsx)$": "ts-jest"
    }
  }
}
